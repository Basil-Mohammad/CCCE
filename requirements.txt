# Pinned per Master Prompt V3 Section 71/72.
# torch/gymnasium/stable-baselines3/mujoco: became installable partway
# through this project (see docs/DEVIATIONS.md #10 for the full, honest
# account -- they were confirmed IMPOSSIBLE to install in two earlier
# sessions of this same project). Only E08_cross_algorithm currently uses
# this stack; all other experiments use the from-scratch NumPy substitute
# described in docs/DEVIATIONS.md #2, which remains necessary documentation
# even though the underlying constraint that motivated it has partially lifted.
numpy==2.4.4
scipy==1.17.1
pandas==3.0.2
matplotlib==3.10.8
pytest==9.1.1
scikit-learn==1.8.0
PyYAML==6.0.3
torch==2.14.0
gymnasium==1.3.0
stable-baselines3==2.9.0
mujoco==3.12.0
